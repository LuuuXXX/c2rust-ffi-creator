# C 项目完整分析报告模板

> **使用说明**：
> 1. 运行 `scripts/analyze_c_project.py <项目根目录>` 生成初始骨架。
> 2. 人工逐节核查、补全所有 TODO 项。
> 3. 在文末「⚠ 人工确认点 0」中完成签字。
> 4. **签字完成后方可推进至 Step 1（头文件扫描）**。
>
> **重要原则**：转换过程必须参考 C 的完整实现，而非只针对接口签名。
> 本文件的目的是确保在动手写任何 Rust 代码之前，已对以下内容有完整认知：
> - 构建系统与产物
> - 实际导出符号（来自二进制，而非仅来自头文件）
> - 现有测试覆盖情况
> - 每个北向函数的完整实现依赖（内部调用链、全局状态、数据类型）

---

## 项目信息

| 字段 | 值 |
| --- | --- |
| 项目名称 | TODO |
| 分析版本 / Commit | TODO |
| 分析人 | TODO |
| 分析日期 | TODO |
| 项目根目录 | TODO |
| 头文件目录 | TODO |

---

## 1. 构建系统分析

### 1.1 构建系统识别

| 构建系统 | 文件 |
| --- | --- |
| TODO | TODO |

### 1.2 构建目标

| 目标名 | 类型（library/executable） | 说明 |
| --- | --- | --- |
| TODO | TODO | TODO |

### 1.3 构建信息（必填）

- **构建命令**（从零开始、生成最终产物的完整命令）：
  ```bash
  TODO
  ```
- **最终产物路径**：`TODO`
- **产物类型**：`staticlib (.a)` / `cdylib (.so/.dll)` / 两者均有
- **编译选项**（如 -O2 / -g / ASAN / UBSAN 等）：`TODO`
- **目标平台矩阵**：
  - [ ] Linux x86_64
  - [ ] Linux aarch64
  - [ ] macOS x86_64
  - [ ] macOS arm64
  - [ ] Windows x86_64
  - [ ] 其他：TODO
- **外部依赖**（链接的第三方库）：`TODO`

---

## 2. 项目文件清单

| 类别 | 数量 | 备注 |
| --- | --- | --- |
| C 源文件（.c） | TODO | TODO |
| 头文件（.h） | TODO | TODO |
| 测试文件 | TODO | TODO |
| 实现文件（非测试） | TODO | TODO |

**测试文件列表**：

| 测试文件路径 | 测试框架 | 说明 |
| --- | --- | --- |
| TODO | TODO（gtest / cmocka / Unity / 自研 / 无） | TODO |

---

## 3. 北向接口声明（来自头文件）

> 以下是从头文件中提取的函数声明。请在「接口类型」列标注「北向（对外）」或「内部（仅内部）」，
> 并删除非北向函数行，确保本表只保留最终需要由 Rust 侧实现的函数集合。

| 函数名 | 所在头文件 | 完整 C 签名 | 接口类型 |
| --- | --- | --- | --- |
| TODO | TODO | TODO | TODO（北向 / 内部） |

**确认要求**：
- [ ] 已与产品/接口负责人确认北向接口范围
- [ ] 已标注哪些函数是 deprecated（Phase 1 是否仍需支持）
- [ ] 已标注哪些函数是 internal（不需要 Rust 侧导出）

---

## 4. 实际导出符号（来自已构建二进制）

> ⚠ **必须实际构建并运行符号检查**，不能仅凭头文件推断导出符号集合。
>
> 运行以下命令提取导出符号：
> ```bash
> # Linux .so
> nm -D --defined-only <产物路径> | grep ' T '
> # Linux .a
> nm --defined-only <产物路径> | grep ' T '
> # macOS .dylib
> nm -D <产物路径> | grep ' T '
> # Windows .dll（需 dumpbin）
> dumpbin /EXPORTS <产物路径>
> ```

| 符号名 | 头文件声明 | 是否需要Rust侧导出 | 备注 |
| --- | --- | --- | --- |
| TODO | ☐ 是 / ☐ 否 | ☐ 是 / ☐ 否 | TODO |

**差异分析**：

- **仅存在于二进制、未在头文件中声明的符号**（如有，需说明原因）：
  - TODO

- **头文件中声明但未出现在二进制导出中的符号**（如有，需说明原因）：
  - TODO

**确认要求**：
- [ ] 已实际构建二进制
- [ ] 已运行 `nm`/`objdump` 并将结果填入上表
- [ ] 头文件声明与二进制导出的差异已分析并解释
- [ ] Rust 侧必须导出的符号集合已最终确认

---

## 5. 现有测试覆盖映射

> 本节确认现有 C 测试的范围，为后续差分测试分级（Step 4）提供依据。
> **无测试覆盖的北向接口属于高风险项，必须在验证计划中特别处理。**

| 测试文件 | 测试框架 | 测试用例（节选） | 覆盖的北向接口 |
| --- | --- | --- | --- |
| TODO | TODO | TODO | TODO |

### 测试运行方式

```bash
# 如何运行所有测试
TODO

# 如何运行单个测试
TODO

# 预期输出（全部通过时）
TODO
```

### 覆盖率汇总

| 北向接口 | 是否有测试覆盖 | 测试用例名 | 覆盖类型（正常路径/错误路径/边界条件） |
| --- | --- | --- | --- |
| TODO | ☐ 有 / ☐ 无 / ☐ 部分 | TODO | TODO |

**整体测试覆盖率估算**：TODO%

**无测试覆盖的高风险接口**（需在 `compatibility-validation-plan.md` 中特别标注）：

- TODO

**确认要求**：
- [ ] 已运行现有测试套件，确认全部通过（或记录已知失败项）
- [ ] 每个北向接口的测试覆盖情况已逐一标注
- [ ] 无测试覆盖的接口已识别并标记为高风险

---

## 6. 北向函数实现依赖分析

> **核心原则**：转换不能只看接口签名，必须完整理解每个函数的实现逻辑。
> 本节要求将每个北向函数的实现依赖梳理清楚，作为后续 Spec v1 填写的输入。

### 6.1 每个北向函数的实现分析

> 对每个北向函数，分析其完整实现并填写以下子表。

#### `TODO: 函数名`

**实现文件**：`TODO`

**实现逻辑摘要**（关键逻辑步骤，3-8 条）：
1. TODO
2. TODO
3. TODO

**直接调用的内部函数**：

| 内部函数名 | 所在文件 | 作用 | 是否也需要迁移/适配 |
| --- | --- | --- | --- |
| TODO | TODO | TODO | ☐ 是 / ☐ 否 |

**依赖的全局/静态变量**：

| 变量名 | 类型 | 读/写 | 初始化时机 | 线程安全问题 |
| --- | --- | --- | --- | --- |
| TODO | TODO | TODO | TODO | TODO |

**依赖的数据类型**（参数/返回值/内部使用的结构体、枚举、typedef）：

| 类型名 | Kind | 是否跨FFI边界传递 | 完整C定义是否已获取 |
| --- | --- | --- | --- |
| TODO | struct/enum/typedef | ☐ 是 / ☐ 否 | ☐ 是 / ☐ 否 |

**边界行为与已知怪癖**：
- TODO

---

### 6.2 全局变量与静态变量清单

| 变量名 | C 类型 | 初始值 | 依赖该变量的北向函数 | 线程安全方案 |
| --- | --- | --- | --- | --- |
| TODO | TODO | TODO | TODO | TODO（Mutex / Once / 无需 / ...） |

### 6.3 完整数据类型清单

> 列出所有**跨 FFI 边界传递**的数据类型（结构体、枚举、typedef、函数指针类型）。
> 每个类型的完整 C 定义必须已获取，在 Spec v1 中将逐一填入布局信息。

| 类型名 | Kind | 完整 C 定义（或文件位置） | 含位域 | 跨平台问题 | Rust 对应类型初步设计 |
| --- | --- | --- | --- | --- | --- |
| TODO | struct/enum/typedef/fn_ptr | TODO | ☐ 是/☐ 否 | TODO | TODO |

### 6.4 头文件依赖与条件编译

**涉及的系统头文件**（可能影响类型定义）：
- TODO

**涉及的条件编译宏**（影响接口行为的 `#ifdef`）：

| 宏名 | 默认值 | 影响的函数/类型 | Rust 侧处理策略 |
| --- | --- | --- | --- |
| TODO | TODO | TODO | TODO（feature flag / 常量 / ...） |

---

## 7. 风险汇总

> 根据以上各节分析，汇总所有发现的风险项。

| 风险编号 | 风险描述 | 风险级别（高/中/低） | 影响的北向接口 | 计划处置策略 |
| --- | --- | --- | --- | --- |
| RISK-01 | TODO（例如：无测试覆盖的接口） | TODO | TODO | TODO |
| RISK-02 | TODO（例如：全局状态线程安全） | TODO | TODO | TODO |
| RISK-03 | TODO（例如：二进制符号与头文件不一致） | TODO | TODO | TODO |

---

## ⚠ 人工确认点 0（Step 0 完成确认）

> **阻断门**：以下清单未全部打勾并签字之前，**不得推进至 Step 1（头文件扫描）**。
> 任何未打勾的项目必须有明确的处置说明或豁免理由，由审查人确认后方可绕过。

### 必须完成的核查项

**构建系统**
- [ ] 构建命令已确认，可在干净环境中成功构建出目标产物
- [ ] 产物形式（staticlib / cdylib）已确认，文件名已记录
- [ ] 目标平台矩阵已确认
- [ ] 外部依赖（链接库）已清点

**导出符号（必须实测，不可跳过）**
- [ ] 已实际构建二进制
- [ ] 已运行 `nm`/`objdump` 提取导出符号并填入第 4 节
- [ ] 导出符号已与头文件声明逐一核对，所有差异已记录并说明
- [ ] Rust 侧必须导出的符号集合已最终确认（即 Phase 1 的输出目标）

**测试现状（必须实测，不可跳过）**
- [ ] 现有测试已成功运行，结果已记录（通过数 / 失败数 / 跳过数）
- [ ] 每个北向接口的测试覆盖情况已逐一标注
- [ ] 无测试覆盖的接口已识别并标记为高风险

**完整实现分析（所有北向函数，不得遗漏）**
- [ ] 每个北向函数的实现文件已定位
- [ ] 每个北向函数的实现逻辑已梳理（第 6.1 节各子表已填写）
- [ ] 每个北向函数的直接与间接调用依赖已梳理完毕
- [ ] 所有跨 FFI 边界的数据类型已清点（第 6.3 节已填写）
- [ ] 全局变量和静态变量已清点，线程安全方案已初步确认
- [ ] 条件编译分支已识别，Rust 侧处理策略已初步确认

**文件完整性**
- [ ] 本文件中所有 TODO 标记已填写或明确标注「不适用（N/A）+ 理由」
- [ ] 第 7 节风险汇总已填写
- [ ] 本文件已保存至项目文档目录（如 `docs/c-project-analysis.md`）

### 签字记录

| 字段 | 值 |
| --- | --- |
| 分析人 | TODO |
| 审查人 | TODO |
| 签字日期 | TODO（YYYY-MM-DD） |
| 未解决高风险项 | TODO（无则填"无"） |
| 确认状态 | ☐ 未完成 / ☐ **已完成，允许推进 Step 1** |

> **签字意味着**：C 项目的构建方式、实际导出符号、测试覆盖现状、
> 每个北向函数的完整实现依赖（调用链、全局状态、数据类型）已全面分析并记录，
> 结果可作为 Step 1 头文件扫描、Spec v1 填写和 ABI 冻结的输入依据。
